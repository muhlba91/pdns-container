# pdns base configuration
config-dir=/etc/pdns
include-dir=/etc/pdns/conf.d
socket-dir=/run/pdns

daemon=no
guardian=yes
version-string=anonymous

load-modules=libgsqlite3backend.so
launch=gsqlite3
gsqlite3-dnssec=no

loglevel=4
